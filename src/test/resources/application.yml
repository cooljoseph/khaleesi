spring:
  jpa:
    hibernate:
      ddl-auto: none

  datasource:
    url: jdbc:h2:mem:khaleesi;MODE=MySQL
    username: sa
    password:
    driver-class-name: org.h2.Driver

  flyway:
      locations: classpath:db/migration/{vendor}